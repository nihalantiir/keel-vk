# packages/

Reserved for the future content-pack format (mounted asset packs for
games built on keel-vk). Nothing is loaded from here yet; see
`docs/Extending.md` for where this fits relative to `src/keel-vk/`.
