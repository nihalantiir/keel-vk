#pragma once

// Generated by CMake's configure_file() from this project's own
// project() call, at configure time - do not edit the generated
// keel/Version.h directly, edit CMakeLists.txt's project() version (or,
// in a fork, whatever project() call now owns this) instead. Exists so
// a window title or overlay string can never drift from the version
// CMake itself thinks this is being built as.

namespace keel {

constexpr int kVersionMajor = @PROJECT_VERSION_MAJOR@;
constexpr int kVersionMinor = @PROJECT_VERSION_MINOR@;
constexpr int kVersionPatch = @PROJECT_VERSION_PATCH@;
constexpr const char* kVersionString = "@PROJECT_VERSION@";

} // namespace keel
