# src/samples/

Reserved for standalone sample programs built on top of keel-vk once there
is more than one thing worth demonstrating separately from the client (for
example, an isolated Clay UI sample, or a networking sample once keel-net
exists). Empty for this landing: the cube in `src/client/` is the only
sample, and it doubles as the smoke test that the bootstrap works.
