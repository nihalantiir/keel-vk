// CI-only fixture, never built (see CMakeLists.txt in this directory) -
// only needs to configure and have its headers resolve.
#include <keel/foundation.hpp>
#include <keel/keel.hpp>

#include "renderer/Renderer.h"

int main() {
    return 0;
}
